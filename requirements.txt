# ============================================================
# Sensor Tower Pipeline - Python Dependencies
# Python 3.13+ required (tested on 3.13.14)
# Install: pip install -r requirements.txt
# ============================================================

# ---- HTTP & API clients ----
requests>=2.32,<3.0            # Đồng bộ HTTP client (Steam, iTunes, IGDB)

# ---- HTML parsing (gacha revenue: parse table từ monthly HTML report) ----
beautifulsoup4>=4.12           # Parser cho HTML table (revenue top 50)
lxml>=5.0                      # Backend nhanh cho bs4

# ---- API server (serve data cho Power BI qua LAN/cloudflare tunnel) ----
fastapi>=0.115,<1.0            # REST API framework
uvicorn[standard]>=0.30        # ASGI server (chạy API)

# ---- Retry & resilience ----
tenacity>=9.0,<10.0            # Exponential backoff cho rate-limited APIs

# ---- Config & env ----
python-dotenv>=1.0,<2.0        # Load .env
PyYAML>=6.0,<7.0               # Đọc sources.yaml

# ---- Data processing ----
pandas>=2.2,<3.0               # Transform raw → dim/fact
pyarrow>=18.0                  # Parquet/Arrow backend cho pandas (binary wheel, không cần build)

# ---- Storage ----
# sqlite3 là stdlib, không cần cài riêng

# ---- Logging & CLI ----
loguru>=0.7.2,<0.8             # Logging đẹp hơn stdlib
click>=8.1,<9.0                # CLI commands (run_daily.py)

# ---- Date/time ----
python-dateutil>=2.9,<3.0      # Timezone, parsing

# ---- Dashboard (data preview / inspection) ----
streamlit>=1.40,<2.0           # Local web dashboard để inspect crawled data
plotly>=5.24,<6.0              # Interactive charts cho streamlit
